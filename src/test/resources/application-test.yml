# 集成测试 / 上下文加载：使用内存 H2，避免依赖外部 MySQL
spring:
  datasource:
    url: jdbc:h2:mem:tms_it;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
